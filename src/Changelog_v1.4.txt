Lufia 2 Auto Tracker v1.4 - Changelog
=======================================

Core Changes
------------
*   **Framework Migration**: Ported entire application from Tkinter to PyQt6.
*   **Architecture**: Refactored into a Domain-Driven Design:
    *   `LogicEngine`: Handles accessibility rules and item requirements (decoupled from UI).
    *   `StateManager`: Central source of truth for Inventory, Locations, and Characters.
    *   `DataListener`: Background TCP listener for external auto-tracking data.

UI Improvements
---------------
*   **Docking System**: implemented a flexible DockWidget system permitting users to rearrange panels.
    *   Restored "T-Shape" layout default (Characters top, Map/Tools split bottom).
    *   Added "Fluid" resizing policy allowing the Map to take priority when resizing.
*   **Customization**:
    *   **Header Colors**: Added Options menu to pick custom colors for Dock titles.
    *   **Font Size control**: Added global font scaling buttons (+/-) to dock headers affecting all inner content.
*   **Map Widget**:
    *   Smooth scaling with QGraphicsView.
    *   Context Menus on Right-Click (Item Search for Cities, Character Assign for Locations).
*   **Inventory**:
    *   Used `FlowLayout` for Tools/Keys to allow wrapping.
    *   Reverted grayscale icons to low-opacity (15%) for better visibility.

New Features
------------
*   **Auto-Tracking**: Integration for receiving game state via TCP (e.g. from USB2SNES helper).
*   **Item Search Dialog**:
    *   Standalone window (non-modal or modal options).
    *   **City Picker**: Dropdown to quickly switch target shops.
*   **Character Assignment**:
    *   Logic to assign characters to locations (e.g. "Found Guy at Alunze").
    *   Visual sprites appear on the map at the assigned location.
*   **Persistence**: Full Save/Load State functionality using JSON.

v1.4.1 Update - Free Placement & Polish
---------------------------------------
*   **Free Placement System**:
    *   **Edit Layout Mode**: Enabled via Options > "Edit Layout". Allows simple drag-and-drop of ANY icon in Characters, Maidens, Tools, and Keys widgets.
    *   **Layout Persistence**: Custom positions are saved automatically to `layout_config.json` and restored on launch.
    *   **Scrollable Docks**: Characters widget now uses a ScrollArea, allowing it to be resized smaller than its content.
*   **Auto-Tracking**:
    *   **Granular Filters**: Added toggles in the "Auto" menu to enable/disable tracking for specific categories (Chars, Tools, Keys, Maidens, Position).
*   **Map**:
    *   **Player Color**: Added "Pick Player Color" option to customize the tracker arrow.

v1.4.2 Update - Map & Marker Enhancements
-----------------------------------------
*   **Player Marker**:
    *   **Size Control**: Adjustable player marker size (1x to 4x) via "Tracker > Player Size".
    *   **Sprite Mode**: Option to use the Active Party Leader's sprite as the player marker ("Tracker > Player Shape").
    *   **Logic**: Sprite marker automatically updates when the in-game party leader changes (via auto-tracking).
*   **Map Fidelity**:
    *   **Improved Scaling**: Implemented `SmoothPixmapTransform` to reduce pixelation artifacts when resizing the map.
    *   **Responsive Resizing**: Fixed issue where map contents did not scale correctly with the window.
    *   **Capsule Logic**: Implemented accurate order-based mapping for Capsule Sprites (matching v1.3 logic).
*   **Layout Refinements**:
    *   **Dock Sizing**: Constrained Items and Hints widgets to prevent them from consuming excessive screen space.
    *   **Manual Assignment**: Restored context menus for manually assigning characters to locations.

v1.4.3 Update - Emulator Compatibility
--------------------------------------
*   **Expanded Emulator Support**:
    *   **Snes9x-nwa**: Added support for split-memory bank layouts (Dungeon Flags at 0xE2...).
    *   **bsnes**: Verified compatibility.
*   **Robust Scanning**:
    *   New "Dual Scan" logic dynamically identifies WRAM and ROM regions regardless of emulator memory model.
    *   Sprite Table scanning ensures correct ROM mapping even in Headerless/Heap-based environments.
*   **Fixes**:
    *   Fixed issue where "Cleared Dungeons" were not detected on NWA versions.
    *   Improved config file loading reliability.

v1.4.5 Update - UI Refinements & Fixes
------------------------------------------
*   **Quality of Life**:
    *   **Scalable Windows**: Converted Maidens and Characters panels to QGraphicsView, allowing contents to scale gracefully when their Dock borders are resized.
    *   **Icon Sizing**: Added dedicated (+/-) scaling buttons for picture sizes across all icon-based widgets, including Characters, Maidens, Tools, and Keys.
    *   **Recover Widgets**: Centralized accidentally closed docks into a custom 'Restore Closed Windows' menu options list.
    *   **Floating Widgets**: Resolved a bug where undocked, floating panels failed to save and restore their screen positions upon restarting the tracker, and ensured they close fully when the main application shuts down.
    *   **Location Text Toggle**: You can now toggle the text labels identifying where characters are found on/off inside the Custom menu.
    *   **City Search Bar**: Formatted the Item Search city dropdown as an editable Auto-Complete typing field.
    *   **Auto Tracking Menu**: Transferred the Auto Checkboxes out of the top ribbon into a dedicated `Tracker` -> `Auto` sub-menu.
    *   **Cleaned Dropdowns**: Purged unreachable game locations (Gordovan, Agurio, Pico Woods) from Item Select tables.
*   **Performance Optimizations**:
    *   **Positional Caching**: Heavily trimmed down Auto-Tracking positional update packets. The tracker now sends a minimal coordinate payload rather than re-computing the entire game inventory every frame a player moves, drastically reducing Python parsing overhead.
*   **Auto-Tracking & Stability**:
    *   **Duplicate Sprite Placements**: Resolved an issue where characters and capsules from auto-tracking could mistakenly duplicate across multiple map locations simultaneously.
    *   **Memory Parsing Fix**: Fixed a critical 64-bit to 32-bit truncation issue in the C# Tracker Helper that caused memory offsets to fail or crash unexpectedly.
    *   **Connection Resilience**: Upgraded tracking sockets to utilize explicit reuse flags (`SO_REUSEADDR`), ensuring rapid tracker restarts do not trigger Windows port blocked errors.
    *   **UI Freeze Safety**: Guarded Python data streams with safe traceback loggers, preventing the main UI from permanently hanging "Scanning in process..." during an unexpected C# helper crash.

v1.4.6 Update - Emulator Discovery & Recovery
----------------------------------------------
*   **Validated Discovery**:
    *   Emulator process names are now hints rather than proof of compatibility.
    *   Every candidate process must pass Lufia II WRAM structure checks before attachment.
    *   Multiple emulator processes are probed instead of accepting the first matching executable.
*   **Broader Compatibility**:
    *   Core tracking can attach from a verified WRAM block without requiring a compatible ROM allocation.
    *   ROM-dependent capsule sprite features remain optional and are enabled only after validation; spoiler-log discovery uses its own signature scan.
    *   Added discovery aliases for Mesen, ares, and BizHawk/EmuHawk while preserving Snes9x and bsnes behavior.
*   **Reliability**:
    *   Memory scans are chunked to avoid allocating entire large process regions at once.
    *   Repeated required-memory read failures trigger automatic detachment and rescanning.
    *   Emulator profile and dungeon configuration paths now resolve deterministically in source and packaged builds.
    *   Added structured helper status messages for searching, probing, attachment, loss, and errors.
*   **Verification**:
    *   Added deterministic helper self-tests for WRAM validation, dynamic profiles, configuration loading, and dungeon mappings.

v1.4.7 Update - Canonical Memory Roots
--------------------------------------
*   **One Game Memory Map**:
    *   Centralized every WRAM and ROM offset in `Lufia2MemoryMap`.
    *   All tracker reads now resolve from a verified WRAM root and optional ROM root.
    *   Removed emulator-specific field addresses from the runtime read path.
*   **Fallback Simplification**:
    *   Replaced full emulator profiles with single-address root hints.
    *   Standard and NWA Snes9x hints reconstruct the WRAM root from the known gold address and then use the canonical map.
    *   Retained the original curated address table as archival reference only.
*   **ROM Portability**:
    *   Removed the fixed Snes9x WRAM-to-ROM distance guess.
    *   ROM roots now come from game signatures and capsule-table validation across all supported layouts.
*   **Verification**:
    *   Added SNES bus-address conversion and root-resolution self-tests.
    *   Added `--root-hints-only` to the live verification harness.
    *   Re-verified standard Snes9x 1.62.3, Snes9x 1.62.3-nwa, and bsnes-plus v05 with real state payloads.

v1.4.8 Update - Compressed Standalone Release
---------------------------------------------
*   **Smaller Helper**:
    *   Enabled .NET trimming and single-file compression while retaining a self-contained Windows runtime.
    *   Replaced reflection-based JSON with compile-time generated metadata so optimized builds preserve the wire protocol.
*   **Lower Runtime Overhead**:
    *   Replaced per-cycle JSON snapshot comparisons with direct collection and dictionary comparisons.
*   **Verification**:
    *   Added protocol self-tests for state and tracker-status JSON property names.
    *   Run the same self-tests from both the trimmed helper and the final packaged executable.

v1.4.9 Update - Diagnostics, State Safety & Flexible Layouts
-----------------------------------------------------------
*   **Diagnostics**:
    *   Added rotating `tracker.log` and `error.log` files under the user's local application-data directory.
    *   Added uncaught Python/thread hooks, Qt message capture, helper stream routing, and contextual memory-read errors.
    *   Added Help > Open Log Folder.
*   **Tracker Reliability**:
    *   Incomplete memory snapshots are logged for diagnosis but never transmitted as game state.
    *   Empty or duplicate party snapshots are rejected; the UI preserves its last valid inventory and characters.
    *   Removed duplicate Python processing of every helper payload and made one-shot Sync wait for a valid snapshot.
*   **Dungeon Interaction**:
    *   Manual dungeon clicks now toggle only cleared state; red/green accessibility remains derived from logic.
*   **Layouts**:
    *   Saved coordinates are scale-independent so image spacing grows and shrinks proportionally.
    *   Added optional placement grids, snap-to-grid, grid sizes, and per-canvas/all-canvas auto-align.
    *   Retained unrestricted free placement when snapping is disabled.
    *   Moved writable layout files out of the packaged temporary directory into local application data.
    *   Added a persistent View option to hide active human party members from the Characters canvas.
*   **Windows**:
    *   Floating docks now use normal window flags and no longer behave as parent-owned tool windows.

v1.4.10 Update - Consistent Canvas Resizing
-------------------------------------------
*   Replaced transform-based Character/Maiden shrinking with real-size scrollable canvases.
*   Made Character/Maiden font and icon controls affect the rendered content while docked.
*   Recomputed cell geometry after font, icon, and location-text changes to prevent clipping.
*   Made reset/restored picture positions honor the active snap grid.
*   Replaced the shared Qt dock-splitter tree with a free panel workspace.
*   Every panel now has independent position, width, and height; resizing one cannot resize another.
*   Retained panel dragging, pinning, hiding/restoring, detach/snap-back, and persistent geometry.
*   Removed the 350 px width caps from Items and Hints.
*   Added a separate Canvas Grid with independent visibility, size, and panel move/resize snapping.
*   Grid-size selection now changes only the grid ruler and never reflows or resizes existing content.
*   Added Layout > Reset Window Layout for rebuilding the factory panel arrangement.

v1.4.11 Update - Character Clarity & Focused Menus
---------------------------------------------------
*   **Release status**: latest experimental build; stable designation pending broader emulator and UI validation.
*   Obtained characters now remain fully lit whether active or inactive; location notes carry recruitment context.
*   Active party members retained after a one-shot Sync can be manually toggled off/on like other characters.
*   Continuous Auto tracking continues to restore live emulator character state on subsequent snapshots.
*   Renamed Edit Layout to Edit Icon Positions to accurately describe its scope.
*   Grouped Layout into Icon Placement and Panel Arrangement.
*   Grouped View into Panel Controls, Character Display, and Map Sprites.
*   Grouped Style into Panel Appearance, Player Marker, and Map Locations.
*   Reworked the in-app User Guide for the current menus, grids, independent panels, character behavior, syncing, and persistence.
